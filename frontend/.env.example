# Server-side backend origin used by the Next.js /api rewrite.
BACKEND_URL=http://127.0.0.1:5000

# Optional: bypass the rewrite and call a public backend /api URL directly.
# NEXT_PUBLIC_API_URL=https://api.example.com/api
