# SCM frontend

See the [project README](../README.md) for setup, backend configuration and validation.

Run `npm run dev` to develop, `npm run build` to compile, and `npm start` to serve a production build. Set `NEXT_PUBLIC_API_URL` when the backend is not at `http://localhost:5000/api`.
